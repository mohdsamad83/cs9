# API Helpers

Shared HTTP clients live here.

- `axiosPublic()` returns the public Axios client for unauthenticated API calls.
- `axisPrivate()` returns the credentialed Axios client for authenticated calls.
